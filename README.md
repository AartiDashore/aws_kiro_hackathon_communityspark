# CommunitySpark 🌍

A local business discovery platform built to spotlight independent and community-owned businesses. Browse businesses, post flash deals with live countdowns, leave reviews, and get AI-powered recommendations — all with zero external API dependencies.

> No Supabase. No Google Maps. No API keys. Just Python + Ollama.

## Tech Stack

- [FastAPI](https://fastapi.tiangolo.com/) — async web framework
- [SQLite](https://www.sqlite.org/) + [aiosqlite](https://github.com/omnilib/aiosqlite) — zero-config async database
- [Jinja2](https://jinja.palletsprojects.com/) — server-side HTML templates
- [Ollama](https://ollama.com/) — local LLM inference (no API key needed)
- [Ruff](https://docs.astral.sh/ruff/) — linting and formatting
- Vanilla JS — no frontend framework needed

## Getting Started

**Prerequisites:** Python 3.11+

```bash
# 1. Clone the repo
git clone https://github.com/your-username/communityspark.git
cd communityspark

# 2. Create and activate a virtual environment
python -m venv venv

# Mac/Linux
source venv/bin/activate
# Windows
venv\Scripts\activate

# 3. Install dependencies
pip install -r requirements.txt

# 4. Start the server
uvicorn main:app --reload
```

Open [http://localhost:8000](http://localhost:8000) — sample data seeds automatically on first run.

## Pages

| Route | Description |
|---|---|
| `/` | Home — browse and search businesses |
| `/deals` | Flash deals with live countdown timers |
| `/register` | Register a new business |
| `/business/{id}` | Business profile with reviews, deals, and photo |
| `/docs` | Interactive API docs (auto-generated by FastAPI) |

## API Overview

All endpoints are prefixed with `/api`.

| Method | Endpoint | Description |
|---|---|---|
| `GET` | `/api/businesses` | List all businesses (supports `?category=` and `?search=`) |
| `POST` | `/api/businesses` | Register a new business |
| `GET` | `/api/businesses/{id}` | Get a business with its deals and reviews |
| `PUT` | `/api/businesses/{id}` | Edit business fields |
| `DELETE` | `/api/businesses/{id}` | Delete a business and all its deals and reviews |
| `POST` | `/api/businesses/{id}/image` | Upload a business photo (JPEG/PNG/WebP, max 5 MB) |
| `DELETE` | `/api/businesses/{id}/image` | Remove the business photo |
| `GET` | `/api/deals` | List all active deals |
| `POST` | `/api/deals` | Post a flash deal |
| `PUT` | `/api/deals/{id}` | Edit a deal (title, discount, time extension, urgency threshold) |
| `DELETE` | `/api/deals/{id}` | Expire/remove a deal |
| `POST` | `/api/reviews` | Submit a review |
| `GET` | `/api/agents/status` | Check Ollama status and installed models |
| `POST` | `/api/agents/generate-story` | AI: generate a founder story |
| `POST` | `/api/agents/match` | AI: find businesses matching a natural language request |

Full interactive docs available at `/docs` when the server is running.

## Business Management

From any business profile page, owners can:

- edit all business fields (name, description, story, category, address, phone, website)
- upload a photo (JPEG, PNG, WebP — max 5 MB, stored in `static/uploads/`)
- remove the photo — the card falls back to a category emoji placeholder
- delete the business entirely (cascades to all deals and reviews)

## Flash Deals

- post a deal with title, discount %, original price, and expiry in hours
- edit a deal after posting — including adjusting the remaining time (positive = extend, negative = shorten)
- set an urgency threshold: when X hours remain, a sticky banner appears at the top of the page reading "⚡ Hurry up! Only Xh Ym remaining before [deal] expires!"
- delete a deal manually at any time
- deals auto-expire via a background task after their set duration

## AI Agents

CommunitySpark includes two local AI agents powered by [Ollama](https://ollama.com/). No API keys or internet connection required.

### Agent 1 — Story Writer

Generates a warm, authentic 3-sentence first-person founder story from basic business info. Used on the registration page to help owners craft their listing.

**Endpoint:** `POST /api/agents/generate-story`

```json
{
  "business_name": "Mama Rosa's Kitchen",
  "category": "Food & Dining",
  "what_you_sell": "Authentic Mexican home cooking",
  "neighborhood": "Mission District, San Francisco",
  "your_background": "Third-generation cook from Oaxaca"
}
```

### Agent 2 — Business Matchmaker

Matches a user's natural language request to the best-fit businesses in the database. Filters by category in Python before calling the LLM, so the AI only reasons over relevant results.

**Endpoint:** `POST /api/agents/match`

```json
{
  "message": "I'm looking for a good haircut for natural hair"
}
```

### MCP Server

An MCP (Model Context Protocol) server is included at `mcp_server.py`, exposing both agents as tools for use inside Kiro or any MCP-compatible client.

```bash
python mcp_server.py
```

### Ollama Setup

```bash
# Install Ollama: https://ollama.com/download
ollama serve
ollama pull llama3
```

The app auto-detects whichever model you have installed. Override with an env var:

```bash
OLLAMA_MODEL=phi3 uvicorn main:app --reload
```

## Project Structure

```
communityspark/
├── main.py               # FastAPI app, routes, lifespan
├── models.py             # Pydantic request/update models
├── database.py           # SQLite setup and seed data
├── mcp_server.py         # MCP server exposing AI agents as tools
├── routers/
│   ├── businesses.py     # CRUD + image upload/delete
│   ├── deals.py          # Create, edit, expire deals
│   ├── reviews.py        # Submit and list reviews
│   └── agents.py         # AI story writer + matchmaker endpoints
├── templates/            # Jinja2 HTML templates
├── static/
│   ├── css/styles.css    # All styles
│   └── uploads/          # Uploaded business photos (auto-created)
└── .kiro/
    ├── steering/         # Project conventions for Kiro
    ├── hooks/            # Automated Kiro hooks
    └── specs/            # Feature specs
```

## Environment Variables

| Variable | Default | Description |
|---|---|---|
| `OLLAMA_URL` | `http://localhost:11434` | Ollama server URL |
| `OLLAMA_MODEL` | `llama3.2` | Model to use for inference |

## Sample Data

On first run, the database is seeded with:

- 5 community businesses across Food, Beauty, Arts, Services, and Retail
- 4 active flash deals with expiry times
- 5 reviews across multiple businesses

The SQLite database file (`communityspark.db`) is created automatically — no setup required.

## Development

```bash
# Lint
ruff check .

# Format
ruff format .
```

## CI/CD

GitHub Actions runs on every push and pull request to `main`:

1. Lint — `ruff check` and `ruff format --check`
2. Deploy — runs after lint passes on pushes to `main`

See [`.github/workflows/ci.yml`](.github/workflows/ci.yml).

## License

[MIT](LICENSE)
